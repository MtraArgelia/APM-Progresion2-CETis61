window.P2Assessment=(function(){const S=[['Requiere apoyo',0,39],['En desarrollo',40,64],['Logrado',65,84],['Dominio avanzado',85,100]];function state(v){if(v==null)return 'Sin evidencia suficiente';return (S.find(x=>v>=x[1]&&v<=x[2])||['Sin evidencia suficiente'])[0]}function avg(a){return a.length?Math.round(a.reduce((s,x)=>s+x.score*x.weight,0)/a.reduce((s,x)=>s+x.weight,0)):null}function summarize(ev){const d={P:[],R:[],M:[],C:[]},g=[],pending=[];(ev||[]).forEach(e=>{let sc=typeof e.score==='number'?Math.max(0,Math.min(100,e.score)):null,w=Number(e.weight||1);if(sc!=null&&d[e.dimension])d[e.dimension].push({score:sc,weight:w,id:e.itemId||''});if(e.gradable===false&&sc==null)pending.push(e.itemId||'abierta');else if(sc!=null)g.push({score:sc,weight:w})});let dimension={};Object.keys(d).forEach(k=>{let v=avg(d[k]);dimension[k]={score:v,state:state(v),evidence:d[k].map(x=>x.id)}});return {dimension,grade:avg(g),gradeStatus:pending.length?'Provisional · valoración docente pendiente':'Calculada con evidencias disponibles',pending}}return {state,summarize};})();